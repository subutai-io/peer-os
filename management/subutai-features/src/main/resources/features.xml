<features name="subutai-common" version="${project.version}">

  <feature name="subutai-common" version="${project.version}">
    <feature>subutai-common-core</feature>
    <feature>subutai-common-dependencies</feature>
  </feature>

  <feature name="subutai-common-core" version="${project.version}">
    <bundle>mvn:${project.groupId}/subutai-common/${project.version}</bundle>
  </feature>

  <feature name="subutai-common-dependencies" version="${project.version}">
    <bundle>mvn:com.google.code.gson/gson/2.3</bundle>
  </feature>

</features>
