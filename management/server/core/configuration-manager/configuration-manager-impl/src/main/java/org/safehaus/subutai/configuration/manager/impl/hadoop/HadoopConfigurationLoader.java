package org.safehaus.subutai.configuration.manager.impl.hadoop;


//import org.apache.hadoop.conf.Configuration;


/**
 * Created by bahadyr on 7/2/14.
 */
public class HadoopConfigurationLoader {

    /*public void test() {
        InputStream in = getClass().getClassLoader().getResourceAsStream( "hadoop-1.2.1/core-site.xml" );
        Configuration c = new Configuration();
        c.addResource( in );
        Iterator i = c.iterator();
        for ( Map.Entry<String, String> stringStringEntry : c ) {

            System.out.println(stringStringEntry.getKey() + " = " + stringStringEntry.getValue());

        }
        System.out.println(c.toString());
    }
*/

}
